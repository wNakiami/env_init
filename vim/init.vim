if !empty(glob('~/.vim/base.vim'))
    source ~/.vim/base.vim
endif

if !empty(glob('~/.vim/plugin.vim'))
    source ~/.vim/plugin.vim
endif

if !empty(glob('~/.vim/custom.vim'))
    source ~/.vim/custom.vim
endif

